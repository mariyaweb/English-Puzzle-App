import BaseElement from '../../../../ui/base-element/base-element';
import './sloganImg.css';

export default class SloganImg extends BaseElement {
  constructor() {
    super({ tag: 'div', styles: ['start__slogan'] });
  }
}
